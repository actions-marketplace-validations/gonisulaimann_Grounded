# tool

```console
$ tool run --verbose --output out.txt
$ tool run --verb --no-color
$ tool run --outptu out.txt
$ pip install --upgrade tool
$ tool plugin-cmd --anything
```

Run `tool run --colour` to force colors.
